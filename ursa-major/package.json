{
  "name": "ursa-major",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ursa Major provenance resolver \u2014 joins finished work back to the model generations that fed it and emits a provenance-resolved outcome record.",
  "scripts": {
    "test": "vitest run",
    "resolve": "tsx src/cli.ts",
    "record:fixture": "tsx src/cli.ts --id mini-fixture --final fixtures/mini/final.md --conversations fixtures/mini/conversations --out fixtures/mini/record --generated-at 2026-09-25T00:00:00.000Z"
  },
  "dependencies": {
    "diff": "^8.0.2"
  },
  "devDependencies": {
    "@types/jsdom": "^30.0.0",
    "@types/node": "^22.10.0",
    "jsdom": "^30.1.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
